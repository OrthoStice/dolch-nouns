<%= form_for(@quizzes) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="field">
    <%= f.text_area :student, placeholder: "Student Name" %>
  </div>
  <%= f.submit "Post", class: "btn btn-primary" %>
<% end %>